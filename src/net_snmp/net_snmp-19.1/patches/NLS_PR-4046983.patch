diff -Naur --exclude=cscope.out /tmp/net_snmp.roots/net_snmp/net-snmp/agent/mibgroup/mibII/ipv6.c ./agent/mibgroup/mibII/ipv6.c
--- /tmp/net_snmp.roots/net_snmp/net-snmp/agent/mibgroup/mibII/ipv6.c	2004-07-07 13:31:54.000000000 -0700
+++ ./agent/mibgroup/mibII/ipv6.c	2005-10-28 21:42:03.000000000 -0700
@@ -1225,7 +1225,11 @@
     int             result;
     int             i, j;
     caddr_t         p;
+#if defined(darwin)
+    static struct inpcb in6pcb, savpcb;
+#else
     static struct in6pcb in6pcb, savpcb;
+#endif
     int             found, savnameLen;
 #if defined(__NetBSD__) && __NetBSD_Version__ >= 106250000	/*1.6Y*/
     struct inpcbtable udbtable;
@@ -1397,8 +1401,13 @@
     int             result;
     int             i, j;
     caddr_t         p;
+#if defined(darwin)
+    static struct inpcb in6pcb, savpcb;
+    struct tcpcb   tcp6cb;
+#else
     static struct in6pcb in6pcb, savpcb;
     struct tcp6cb   tcp6cb;
+#endif
     int             found, savnameLen;
     static int      tcp6statemap[TCP6_NSTATES];
     static int      initialized = 0;
@@ -1638,7 +1647,11 @@
     int             result;
     int             i, j;
     caddr_t         p;
+#if defined(darwin)
+    static struct inpcb in6pcb, savpcb;
+#else
     static struct in6pcb in6pcb, savpcb;
+#endif
     struct tcpcb    tcpcb;
     int             found, savnameLen;
 #if defined(__NetBSD__) && __NetBSD_Version__ >= 106250000	/*1.6Y*/
